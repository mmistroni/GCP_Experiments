{
    "python.analysis.extraPaths": [
        "dataflow/shareloader"
    ],
    "python.testing.unittestEnabled": true,
    "python.testing.unittestArgs": [
        // 1. Set the top-level directory to the root of your workspace ('.'). 
        // This is crucial for fixing the import context.
        "-t", ".", 
        
        // 2. Set the start directory to the *full relative path* to your test folder.
        "-s", "GCP_Experiments/dataflow/shareloader/tests", 
        
        // 3. Keep the standard file pattern.
        "-p", "test_*.py"
    ]
}