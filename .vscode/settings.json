{
    "python.analysis.extraPaths": [
        "dataflow/shareloader"
    ],
    "python.testing.unittestEnabled": true,
    // The relative path from the workspace root where testing should start searching.
    "python.testing.unittestArgs": [
        "-t", "dataflow", // Sets the start directory for discovery to 'dataflow'
        "-s", "shareloader/test", // Sets the pattern for searching within the start directory
        "-p", "test_*.py" // Standard file pattern for unittest
    ]
}